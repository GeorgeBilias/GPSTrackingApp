<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/light_blue</item>

        <!-- Defining that new color in ColorPrimaryDark -->
        <item name="colorPrimaryDark">@color/light_blue</item>
        <item name="colorAccent">@color/light_green</item>



        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="ButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/dark_blue</item> <!-- Default text color -->
    </style>
    <style name="TextNew" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/dark_blue</item> <!-- Default text color -->
    </style>
    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />
</resources>
